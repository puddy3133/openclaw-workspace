# 2026-03-21 - 周六

> 北京时间 2026/3/21 02:00:14 自动创建

## 启动检查

- [ ] 昨日记忆日志检查
- [ ] 今日日志创建：完成
- [ ] TASKS.md 检查


## 昨日回顾 (2026-03-20)

## 今日计划

**待办：**
- [ ] （暂无待办任务，请检查 TASKS.md）

## 自我进化准备

- [ ] 检查昨日执行效率
- [ ] 识别可优化流程
- [ ] 更新 MEMORY.md 如有新发现


## 昨日回顾 (2026-03-20)

## 今日计划

**待办：**
- [ ] （暂无待办任务，请检查 TASKS.md）

## 自我进化准备

- [ ] 检查昨日执行效率
- [ ] 识别可优化流程
- [ ] 更新 MEMORY.md 如有新发现

## 会话记录

### 09:55 CST — 飞书插件修复确认
- **内容**：飞书插件修复成功验证
  - 昨晚修改 `plugins.allow` 中 `"feishu"` → `"feishu-openclaw-plugin"`
  - Gateway 已重启，feishu_doc/chat/wiki/drive/bitable 全部正常注册
- **状态**：✅ 已完成

### 01:58 CST — 国栋回来 + 全面系统检查
- **参与者**：国栋（Zz）
- **内容**：
  - 国栋升级 OpenClaw 到 2026.3.13，执行了 `openclaw doctor --fix`
  - 国栋尝试修改飞书插件实现小乔-大乔直接对话，未成功，飞书插件一度失效
  - 小乔进行了全面系统检查（openclaw doctor + 手动审计）
  - 修复项：
    - 删除 `star-office-sync` 残留目录
    - 删除 `memory/YYYY-MM-DD.md` 模板文件
    - Gateway 绑定从 `lan`(0.0.0.0) 改为 `loopback`
    - `plugins.allow` 去重（删除多余的 `"feishu"`）
    - `plugins.entries.feishu.enabled` 改为 false（禁用旧版 stock 插件）
  - Cron 任务重新调度：全部移至 11:00-17:30 CST 窗口，安全调优暂停，全部飞书推送
  - 插件分析完成：`simple-cron-plugin` 被新版内置 cron 可能替代
- **待办**：
  - Gateway 需重启使绑定变更生效
  - 确认 `simple-cron-plugin` 是否可移除（需执行 `openclaw cron list`）
- **状态**：进行中（等待 Gateway 重启确认）

---

## 今日复盘

*(23:30 自动复盘后更新)*

## 17:07 健康检查（与国栋对话）

**触发**：国栋要求自身健壮性检查（及时响应、稳定、不失联）

**检查结果**：
- Gateway：运行中，LaunchAgent 正常
- 飞书渠道：ON，状态正常
- 心跳：30 分钟周期正常
- 磁盘：14GB/100GB（55%）
- 系统运行：16 小时 37 分钟

**发现的问题**：
1. Gateway unreachable (missing scope: operator.read) — 不影响飞书消息
2. 记忆功能禁用 (`slots.memory="none"`)
3. 系统负载偏高 (load 4.11)，45 个活跃会话
4. 小模型安全告警（120B 需沙箱+禁用 web）
5. Tailscale 关闭，远程不可用

**国栋的关注点**：记忆降级为何禁用、飞书流式输出设置

**待跟进**：清理旧会话、是否重新启用记忆功能

### 14:13-14:36 EST — 学习彬子记忆系统文章 + 制定优化方案

**参与者**：国栋（Zz）

**任务**：深度学习彬子《拯救龙虾记忆》系列三篇文章，总结长期记忆优化方案

**文章核心内容**：
1. **第一篇：搭建记忆结构底座**
   - 记忆文件分层：MEMORY.md → NOW.md + INDEX.md + lessons/ + decisions/ + projects/
   - 记忆路由规则：踩坑 → lessons/，决策 → decisions/，项目 → projects/
   - 外置笔记系统：Obsidian Vault 存放长文、调研报告

2. **第二篇：构建主被动记忆防线**
   - 三道防线：Memory Flush（系统内置） + 心跳扫描（60分钟） + 夜间反思（凌晨）
   - 强制记忆补录机制：不靠自觉，靠机制

3. **第三篇：打造记忆检索引擎**
   - Memory Core 配置：embedding provider + 混合检索（向量70% + BM25 30%）
   - 参数调优：MMR lambda 0.7，时间衰减半衰期30天
   - QMD 方案：查询扩展 + Reranker + 多 Collection

**对比分析**：
- 当前方案 vs 彬子方案的核心差距：缺少心跳扫描和夜间反思两道防线
- 当前记忆槽禁用（slots.memory="none"），检索能力完全失效

**制定的优化方案**：
- **P0（今天）**：启用记忆槽、创建今日日志
- **P1（3天内）**：建立心跳扫描机制、更新 NOW.md
- **P2（两周内）**：建立夜间反思机制、配置 Memory Search 检索参数
- **P3（持续）**：创建 INDEX.md 索引、过期文件清理机制

**状态**：✅ 方案制定完成，等待国栋确认执行

### 14:50-15:10 EST — 执行 P0-1：启用记忆槽

**任务**：将 `slots.memory: "none"` 改为启用状态

**执行过程**：
1. 定位配置：`slots.memory = "none"` 在 `~/.openclaw/openclaw.json` 第 503 行
2. 尝试修改：`"none"` → `"default"` → 报错 `plugin not found: default`
3. 正确方法：使用 `jq 'del(.plugins.slots.memory)'` 删除整个 memory 配置
4. Gateway 重启成功，`memory-tfidf-plugin` 加载正常

**关键发现**：
- `slots` 是 plugin 槽位配置，`memory: "none"` 表示禁用记忆插件
- 正确的启用方式：删除 `slots.memory` 配置项，让系统使用默认记忆后端
- 系统已有 `memorySearch` 配置（混合检索：向量70% + BM25 30%），删除禁用项后自动生效

**验证结果**：
- Gateway 日志显示 `[memory-tfidf] Plugin registered successfully`
- 记忆检索功能已恢复

**状态**：✅ P0-1 完成

---

## 今日复盘 *(23:30 自动复盘后更新)*
