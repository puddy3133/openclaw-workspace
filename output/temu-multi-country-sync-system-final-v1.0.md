# Temu 多国家同步上架系统 - 完整技术方案

> 文档版本：v1.0 FINAL  
> 更新日期：2026-03-31  
> 适用场景：Temu欧盟25国产品同步  
> 业务规模：日均100+产品，预期增长至1000+

---

## 目录

1. [项目概述](#一项目概述)
2. [需求分析](#二需求分析)
3. [技术方案选型](#三技术方案选型)
4. [系统架构设计](#四系统架构设计)
5. [详细设计方案](#五详细设计方案)
6. [核心功能实现](#六核心功能实现)
7. [异常处理机制](#七异常处理机制)
8. [安全与权限](#八安全与权限)
9. [运维与监控](#九运维与监控)
10. [实施计划](#十实施计划)
11. [附录](#十一附录)

---

## 一、项目概述

### 1.1 项目背景

Temu（拼多多海外版）电商平台要求商家在欧盟25个国家分别上架产品。当前流程存在以下痛点：

- **手动操作繁琐**：每个产品需在25个国家分别填写价格、库存
- **多账号管理复杂**：一人管理多个店铺账号，切换频繁
- **核价周期长**：德国站提交后需等待24-48小时核价
- **数据管理混乱**：价格、库存分散管理，易出错

### 1.2 项目目标

构建自动化系统，实现：

1. **一键同步**：德国站核价通过后，自动同步到24个欧盟国家
2. **多账号管理**：支持多店铺账号自动切换
3. **数据集中管理**：飞书多维表格统一管理25国价格、库存
4. **飞书指令驱动**：通过飞书消息触发同步操作

### 1.3 核心指标

| 指标 | 目标值 |
|------|--------|
| 同步成功率 | > 95% |
| 平均同步耗时 | < 5秒/国家 |
| 日处理能力 | 1000+产品 |
| 数据一致率 | > 99% |

---

## 二、需求分析

### 2.1 业务流程

```
德国站上架 → 平台核价(24-48h) → 获取商品ID → 同步24国
                                              ↓
                                    ┌─────────────────────┐
                                    │ 飞书多维表格管理    │
                                    │ • 25国价格         │
                                    │ • 25国库存         │
                                    │ • 同步状态         │
                                    └─────────────────────┘
```

### 2.2 功能需求

| 功能模块 | 需求描述 | 优先级 |
|----------|----------|--------|
| 核价监控 | 自动检测核价状态，通过后触发同步 | P0 |
| 批量同步 | 支持单产品/多产品批量同步 | P0 |
| 多账号切换 | 自动切换不同店铺账号 | P0 |
| 数据校验 | 同步前校验价格/库存有效性 | P1 |
| 回滚机制 | 支持同步失败后的回滚操作 | P1 |
| 数据一致性 | 定期校验飞书与平台数据一致性 | P2 |

### 2.3 非功能需求

- **可用性**：系统可用性 > 99.5%
- **安全性**：API密钥加密存储，多账号隔离
- **可扩展性**：支持未来扩展到更多国家
- **可维护性**：代码模块化，文档完善

---

## 三、技术方案选型

### 3.1 方案对比

| 方案 | 自动化程度 | 封号风险 | 开发周期 | 月成本 | 推荐度 |
|------|-----------|----------|----------|--------|--------|
| **官方API** | 高 | 无 | 2-3天 | ¥200 | ⭐⭐⭐⭐⭐ |
| **影刀RPA** | 高 | 中 | 1-2天 | ¥1000 | ⭐⭐⭐⭐ |
| **半自动辅助** | 中 | 低 | 0.5天 | ¥0 | ⭐⭐⭐ |
| **云手机集群** | 高 | 低 | 3-5天 | ¥2000 | ⭐⭐⭐ |

### 3.2 推荐方案：官方API + RPA备选

**首选方案：官方API（Skill化）**
- 无封号风险，效率最高
- 飞书指令直接操作
- 长期可持续维护

**备选方案：影刀RPA**
- API不可用时启用
- 浏览器自动化，需防检测措施
- 确保业务连续性

**兜底方案：半自动辅助**
- 任何自动化失效时使用
- 人工+工具辅助

### 3.3 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| 前端交互 | 飞书消息 | 用户指令输入、结果通知 |
| 业务逻辑 | Python + OpenClaw Skill | 指令解析、业务编排 |
| 数据存储 | 飞书多维表格 | 价格、库存、状态管理 |
| 外部接口 | Temu Open API | 商品同步、价格更新 |
| 备选方案 | 影刀RPA + Chrome | 浏览器自动化 |

---

## 四、系统架构设计

### 4.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户层                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   飞书消息  │  │  多维表格   │  │  监控面板   │         │
│  └──────┬──────┘  └─────────────┘  └─────────────┘         │
└─────────┼───────────────────────────────────────────────────┘
          │
┌─────────┼───────────────────────────────────────────────────┐
│         ↓              应用层（OpenClaw Skill）              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Temu Sync Skill                        │   │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌────────┐ │   │
│  │  │指令解析 │→ │业务编排 │→ │API调用  │→ │状态更新│ │   │
│  │  └─────────┘  └─────────┘  └─────────┘  └────────┘ │   │
│  └─────────────────────────────────────────────────────┘   │
└──────────────────────────┬──────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        ↓                  ↓                  ↓
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│ Temu Open API │  │ 飞书多维表格  │  │  影刀RPA备选  │
│ • 商品同步    │  │ • 价格管理    │  │ • 浏览器控制  │
│ • 价格更新    │  │ • 库存管理    │  │ • 自动填表    │
│ • 库存更新    │  │ • 状态追踪    │  │ • 模拟点击    │
└───────────────┘  └───────────────┘  └───────────────┘
```

### 4.2 数据流

```
1. 用户发送指令: @小乔 同步Temu SKU-001 到 欧盟25国
           ↓
2. Skill解析指令，提取SKU和目标国家
           ↓
3. 查询飞书多维表格获取25国价格/库存
           ↓
4. 调用Temu API批量同步到25个国家
           ↓
5. 更新飞书表格同步状态
           ↓
6. 返回结果到飞书消息
```

---

## 五、详细设计方案

### 5.1 飞书多维表格设计

**表名**：Temu产品价格库存管理

#### 核心字段

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 产品标签 | 文本 | ✅ | 内部SKU，主字段 |
| Temu商品ID | 文本 | ✅ | 德国站商品ID |
| 所属账号 | 单选 | ✅ | 账号A/账号B/... |
| 同步状态 | 单选 | ✅ | 待同步/已同步/失败/核价中 |
| 核价状态 | 单选 | ✅ | 核价中/已通过/已拒绝 |

#### 25国价格库存字段

| 国家 | 价格字段 | 库存字段 | 货币 |
|------|----------|----------|------|
| 德国 | DE_价格 | DE_库存 | EUR |
| 法国 | FR_价格 | FR_库存 | EUR |
| 英国 | UK_价格 | UK_库存 | GBP |
| ... | ... | ... | ... |

#### 系统字段

| 字段名 | 类型 | 自动 | 说明 |
|--------|------|------|------|
| 创建时间 | 创建时间 | ✅ | 记录创建时间 |
| 最后同步时间 | 修改时间 | - | 最后同步操作时间 |
| 失败原因 | 文本 | - | 同步失败时记录 |

#### 视图设计

1. **待同步视图**：筛选同步状态="待同步"
2. **按账号分组**：按所属账号分组显示
3. **同步失败**：筛选同步状态="失败"
4. **核价中**：筛选核价状态="核价中"

### 5.2 Temu API接口设计

#### 接口1：获取商品列表

```http
POST /openapi/router
Content-Type: application/json
X-App-Key: {app_key}
X-Timestamp: {timestamp}
X-Sign: {sign}
X-Access-Token: {access_token}

{
  "method": "temu.product.list",
  "params": {
    "status": "approved",
    "page": 1,
    "page_size": 50
  },
  "body": {}
}
```

#### 接口2：同步商品到多国

```http
{
  "method": "temu.product.sync",
  "params": {
    "product_id": "1234567890",
    "source_site": "DE"
  },
  "body": {
    "target_sites": [
      {"site": "FR", "price": 55, "currency": "EUR", "stock": 100},
      {"site": "UK", "price": 45, "currency": "GBP", "stock": 100}
    ]
  }
}
```

#### 错误码

| 错误码 | 说明 | 处理建议 |
|--------|------|----------|
| 0 | 成功 | - |
| 1003 | 授权失败 | 重新授权 |
| 2002 | 价格超出限制 | 调整价格 |
| 5000 | 系统错误 | 稍后重试 |

### 5.3 飞书指令设计

| 指令 | 功能 | 示例 |
|------|------|------|
| 同步Temu | 同步指定产品 | `@小乔 同步Temu SKU-001,SKU-002 到 欧盟25国` |
| 同步全部 | 同步所有待同步产品 | `@小乔 同步Temu 全部待同步 到 德国,法国` |
| 查询状态 | 查询产品同步状态 | `@小乔 查询Temu SKU-001 状态` |
| 回滚 | 回滚到历史版本 | `@小乔 回滚 Temu SKU-001 到 昨天` |

---

## 六、核心功能实现

### 6.1 Skill主逻辑

```python
class TemuSyncSkill:
    def handle(self, message: str, context: dict) -> str:
        # 1. 解析指令
        if "同步Temu" in message:
            return self.handle_sync(message)
        elif "查询Temu" in message:
            return self.handle_query(message)
        elif "回滚" in message:
            return self.handle_rollback(message)
    
    def handle_sync(self, message: str) -> str:
        # 提取SKU和目标国家
        skus = self.extract_skus(message)
        countries = self.extract_countries(message)
        
        # 循环处理每个SKU
        for sku in skus:
            # 读取飞书表格数据
            data = self.bitable.get_product(sku)
            
            # 调用Temu API同步
            result = self.temu.sync(data, countries)
            
            # 更新状态
            self.bitable.update_status(sku, '已同步' if result.success else '失败')
        
        return self.format_reply(results)
```

### 6.2 核价监控定时任务

```python
class PricingMonitor:
    """每小时执行一次"""
    
    def run(self):
        # 查询所有"核价中"的产品
        pending = self.bitable.get_by_status('核价中')
        
        for product in pending:
            # 查询Temu平台实际状态
            status = self.temu.get_product_status(product['temu_id'])
            
            if status['pricing_status'] == 'approved':
                # 核价通过，标记为待同步
                self.bitable.update_status(product['sku'], '待同步')
                send_notification(f"✅ {product['sku']} 核价通过")
```

### 6.3 数据一致性校验

```python
class DataConsistencyChecker:
    """每日凌晨执行全量校验"""
    
    def check_all(self):
        discrepancies = []
        
        for product in self.bitable.get_all():
            temu_data = self.temu.get_product_details(product['temu_id'])
            
            # 比对每个国家的价格
            for country in EU_COUNTRIES:
                if product['prices'][country] != temu_data['prices'][country]:
                    discrepancies.append({
                        'sku': product['sku'],
                        'country': country,
                        'bitable': product['prices'][country],
                        'temu': temu_data['prices'][country]
                    })
        
        if discrepancies:
            send_alert(f"发现 {len(discrepancies)} 处数据不一致")
```

### 6.4 回滚机制

```python
class RollbackManager:
    def record_operation(self, operation):
        """记录每次操作历史"""
        self.history.append({
            'timestamp': datetime.now(),
            'sku': operation['sku'],
            'old_prices': operation['old_prices'],
            'new_prices': operation['new_prices']
        })
    
    def rollback(self, sku, timestamp=None):
        """回滚到指定时间点的价格"""
        history = self.find_history(sku, timestamp)
        
        # 重新同步旧价格
        self.temu.sync(sku, history['old_prices'])
```

---

## 七、异常处理机制

### 7.1 异常分类

| 级别 | 异常类型 | 处理策略 |
|------|----------|----------|
| L1 | API调用异常 | 重试3次，间隔5秒 |
| L2 | 业务逻辑异常 | 记录日志，跳过该产品 |
| L3 | 数据异常 | 终止任务，通知人工 |
| L4 | 系统异常 | 自动重启，恢复任务 |

### 7.2 重试策略

```python
@retry(stop=stop_after_attempt(3), wait=wait_fixed(5))
def call_api_with_retry(method, params, body):
    """API调用带重试"""
    return self.temu.call_api(method, params, body)
```

### 7.3 通知机制

| 场景 | 通知方式 | 接收人 |
|------|----------|--------|
| 同步完成 | 飞书消息 | 操作人 |
| 同步失败 | 飞书消息+邮件 | 操作人+管理员 |
| 核价通过 | 飞书消息 | 操作人 |
| 系统异常 | 飞书消息+电话 | 管理员 |

---

## 八、安全与权限

### 8.1 API密钥管理

- **存储**：AES-256加密存储
- **轮换**：access_token 90天轮换
- **隔离**：不同账号使用不同token

### 8.2 权限矩阵

| 功能 | 管理员 | 运营 | 财务 | 查看员 |
|------|--------|------|------|--------|
| 触发同步 | ✅ | ✅ | ❌ | ❌ |
| 修改价格 | ✅ | ✅ | ❌ | ❌ |
| 查看报表 | ✅ | ✅ | ✅ | ✅ |
| 账号配置 | ✅ | ❌ | ❌ | ❌ |

### 8.3 数据安全

- 传输：全链路HTTPS
- 存储：敏感字段加密
- 审计：全量操作日志

---

## 九、运维与监控

### 9.1 监控指标

| 指标 | 正常范围 | 告警阈值 |
|------|----------|----------|
| 日同步成功率 | > 95% | < 90% |
| 平均同步耗时 | < 3秒 | > 10秒 |
| API错误率 | < 1% | > 5% |
| 数据一致率 | > 99% | < 95% |

### 9.2 成本估算

**API方案**：¥200/月
- 服务器：¥200
- API调用：免费

**RPA方案**：¥1000/月
- 影刀RPA：¥500
- 服务器：¥200
- IP代理：¥300

### 9.3 备份策略

| 数据类型 | 备份频率 | 保留周期 |
|----------|----------|----------|
| 产品价格数据 | 每日 | 30天 |
| 操作日志 | 实时 | 90天 |
| 配置文件 | 变更时 | 永久 |

---

## 十、实施计划

### 10.1 开发排期（6天）

| 天数 | 任务 | 产出 |
|------|------|------|
| Day 1 | 环境准备、API申请 | app_key/app_secret |
| Day 2 | Skill框架开发 | 基础指令解析 |
| Day 3 | API对接、飞书集成 | 核心同步功能 |
| Day 4 | 异常处理、监控 | 完整功能 |
| Day 5 | 测试优化 | 测试报告 |
| Day 6 | 灰度上线 | 上线文档 |

### 10.2 灰度策略

```
Week 1: 内部测试（5个SKU）
Week 2: 小范围灰度（10%产品）
Week 3: 全量上线（100%产品）
```

### 10.3 验收标准

- [ ] 同步成功率 > 95%
- [ ] 平均耗时 < 5秒/国家
- [ ] 日处理能力 > 1000产品
- [ ] 无P0级bug

---

## 十一、附录

### 附录A：完整代码

详见代码仓库：
- `temu_api.py` - API客户端
- `bitable_helper.py` - 飞书表格操作
- `skill.py` - Skill主逻辑
- `pricing_monitor.py` - 核价监控

### 附录B：RPA备选方案

当API不可用时，启用影刀RPA方案：
- 浏览器自动化
- 随机延迟防检测
- Cookie隔离
- 每小时限速50个

### 附录C：常见问题

**Q1：核价被拒怎么办？**
A：系统自动记录原因，通知运营调整价格后重新提交。

**Q2：同步失败如何回滚？**
A：使用 `@小乔 回滚 Temu SKU-001` 指令回滚到上一版本。

**Q3：多账号如何隔离？**
A：每个账号独立access_token，数据权限隔离。

---

**文档结束**

*版本：v1.0 FINAL*  
*更新时间：2026-03-31*  
*作者：OpenClaw AI Assistant*
